server {
    server_name {{ domain }};

    listen {{ port }};
    listen [::]:{{ port }};

    root {{ document_root }};

    location / {
        auth_basic "{{ domain }} archive";
        auth_basic_user_file {{ basic_auth_file }};
        autoindex on;
    }
}

